import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormulariocuatroPage } from './formulariocuatro.page';

describe('FormulariocuatroPage', () => {
  let component: FormulariocuatroPage;
  let fixture: ComponentFixture<FormulariocuatroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormulariocuatroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
